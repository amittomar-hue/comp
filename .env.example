# WordPress REST API base URL (no trailing slash)
WP_API_URL=http://localhost:8080/wp-json

# Secret token for WordPress → Next.js ISR revalidation webhook
REVALIDATE_SECRET=your-secret-here

# (Optional) Override for production
# WP_API_URL=https://cms.compunnel.com/wp-json
